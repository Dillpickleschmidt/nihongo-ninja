# Convex deployment (run `nub run convex:dev` once to provision, then fill these in)
CONVEX_DEPLOYMENT=
VITE_CONVEX_URL=

# Better Auth (set on the Convex deployment: npx convex env set SITE_URL <app url>)
# SITE_URL is read by convex/auth.ts at request time.
SITE_URL=http://localhost:3000
